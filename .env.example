# DeepSeek API key for AI-powered wellness reports (optional)
# Falls back to local template reports if not set.
DEEPSEEK_API_KEY=

# JWT Secret — MUST be a random string in production
JWT_SECRET=

# Set to false in production to suppress verbose SQLAlchemy logs
DEBUG=true
